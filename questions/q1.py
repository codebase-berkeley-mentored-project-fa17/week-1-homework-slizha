def get_max(array):
    """
    Returns the max element of the input array.
    :param array: List of numbers.
    :return: The max number in the array, or None of the array is empty.
    """
    ########################################################
    # Question 1
    # Change the function code so that it works as expected.
    ########################################################
    if not array:
        return None
    return max(array)
    ########################################################
